#pragma once
#include <fstream>
#include <filesystem>
#include <vector>
#include "Parser.h"

/*
class Linker
{
    static constexpr char ext[5] = ".mdb";
    const std::filesystem::path home;
    std::vector<std::filesystem::path> paths;
    std::vector<std::filesystem::path>& find_paths();
    std::vector<std::vector<Symbol>> symbols;
    std::vector<std::vector<Symbol>>& get_all_symbols();
    public:
    explicit Linker(std::string home_dir) : home(home_dir) {}
    std::vector<Symbol> link();
};
*/